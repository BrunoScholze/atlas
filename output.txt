========================================
AGENTE DE CHAMADOS — ANÁLISE DO TICKET
========================================

TICKET   : DMANUFATURA-21274 — facilitar adicionar ggf em apontamento de ordem
DATA     : 2026-06-26 16:00

----------------------------------------
FUNCIONALIDADES IDENTIFICADAS
----------------------------------------
- Editar MOB/GGF (ligado ao Apontamento) — motivo: o botão "editar MOB/GGF" da tela de apontamento é o ponto de entrada do fluxo descrito no ticket
- Apontamento de ordem de produção (fluxo completo) — motivo: é a origem dos horários de início/fim do período de produção que precisam ser passados adiante
- Reporte de GGF/MOB (Overhead Labor Report) — motivo: a tela de criação de GGF é onde o campo `reportedHours` precisa chegar pré-preenchido

----------------------------------------
LOCALIZAÇÃO DO PROBLEMA
----------------------------------------
Arquivo: `overhead-labor-create.component.ts`, linha 175
O método `initReport()` não atribui valor a `_report.reportedHours`, e nenhum dado de hora é passado pelo fluxo de navegação datasul-report-v2 → datasul-overhead-labor-list → overhead-labor-create.

----------------------------------------
CAUSA PROVÁVEL
----------------------------------------
A cadeia de navegação entre os três componentes não inclui os horários de início/fim do apontamento (`startTime`/`endTime`). Sem esses dados chegando ao `overhead-labor-create`, o campo de horas reportadas não pode ser pré-calculado.

----------------------------------------
COMO RESOLVER
----------------------------------------
Calcular `defaultReportedHours` no método `goToOverheadLaborReportPage()` de `datasul-report-v2` e propagar o valor até `initReport()` em `overhead-labor-create`.

DIFF_START arquivo: src/app/report-process/datasul/datasul-report-v2/datasul-report-v2.page.ts linha: 1033
            ttFormularioOrdem: this.ttFormularioOrdem,
            operationCode: this.isSFC ? this.selectedSplit.split('/')[0] : 0,
+           defaultReportedHours: (() => {
+               const ini = (this.startTime || '').replace(':', '');
+               const fim = (this.endTime || '').replace(':', '');
+               if (ini.length < 4 || fim.length < 4) return undefined;
+               const h1 = Number(ini.slice(0, 2)) + Number(ini.slice(2)) / 60;
+               const h2 = Number(fim.slice(0, 2)) + Number(fim.slice(2)) / 60;
+               return h2 > h1 ? h2 - h1 : 1 / 60;
+           })(),
        },
    });
DIFF_END

DIFF_START arquivo: src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/datasul-overhead-labor-list.component.ts linha: 511
            operationCode: this.operationCode,
            routingCode: this.routingCode ? this.routingCode : "",
+           defaultReportedHours: this.defaultReportedHours,
        },
    });
DIFF_END

DIFF_START arquivo: src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/components/overhead-labor-create/overhead-labor-create.component.ts linha: 180
        this._report.referenceCode = this.productionOrder.referenceCode;
        this._report.transactionDate = new Date().toISOString().split('T')[0];
+       if (this.defaultReportedHours) this._report.reportedHours = this.defaultReportedHours;
    }
DIFF_END

Os dois primeiros diffs criam e propagam o valor calculado; o terceiro aplica o valor no momento em que o formulário de criação é inicializado, eliminando a necessidade de preenchimento manual pelo usuário.

----------------------------------------
ARQUIVOS ANALISADOS
----------------------------------------
- src/app/report-process/datasul/datasul-report-v2/datasul-report-v2.page.html
  └─ src/app/report-process/datasul/datasul-report-v2/datasul-report-v2.page.ts (componente)
- src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/datasul-overhead-labor-list.component.html
  └─ src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/datasul-overhead-labor-list.component.ts (componente)
- src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/components/overhead-labor-create/overhead-labor-create.component.html
  └─ src/app/overhead-labor-report/datasul/datasul-overhead-labor-list/components/overhead-labor-create/overhead-labor-create.component.ts (componente)

----------------------------------------
OBSERVAÇÕES
----------------------------------------
- `datasul-overhead-labor-list.component.ts` requer duas alterações de suporte: (a) declarar `public defaultReportedHours: number | undefined;` como propriedade da classe (após linha 54); (b) dentro do bloco `if (!this.fromMenu)` em `ngOnInit()` (após linha 145), adicionar `if (extras.defaultReportedHours !== undefined) this.defaultReportedHours = extras.defaultReportedHours;`.
- `overhead-labor-create.component.ts` requer duas alterações de suporte: (a) declarar `public defaultReportedHours: number | undefined;` como propriedade da classe; (b) dentro do bloco `if (!this.fromMenu)` no construtor (após linha 122), adicionar `if (nav.defaultReportedHours !== undefined) this.defaultReportedHours = nav.defaultReportedHours;`.
- Para o fluxo SFC, `startTime`/`endTime` correspondem ao horário do timer; se necessário cobrir SFC com os campos do formulário, substituir pelas leituras de `hraInicReporte`/`hraFimReporte` de `overheadLaborSFCParameters` (já disponível na mesma função).

========================================